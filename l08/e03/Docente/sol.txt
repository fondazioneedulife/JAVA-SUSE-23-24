class Solution {
    public static String whoLikesIt(String... names) {
        if(names.length==0)
          return "no one likes this";
        
        if(names.length ==1){
          return names[0]+" likes this";
        }
      
        // ecc.
        /*
          String a = "";
          for (String b : names){
            a+=b;
          }.
        */
    }
}